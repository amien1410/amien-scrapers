# setup.py — kept intentionally thin.
# All authoritative metadata lives in pyproject.toml (PEP 517/518/621).
# This shim exists only for legacy tooling that cannot read pyproject.toml.

from setuptools import setup

setup()
